- 你来介绍一下你的情况和项目经历吧。
- 你平时会研究模型框架的代码吗？
- 我看你对 pi 有了解，你讲一下吧。
- 看你用了扩散模型，现在大家都说 End-to-End，你为什么要把 VLM 和 Diffusion 解耦？如果我直接用一个大的多模态模型输出坐标点，你觉得在手术这种精度场景下，最大的瓶颈会出现在哪？
- 扩散模型生成的是 Action Chunk，但在手术中，组织形变是实时的。如果你的 Chunk 还没跑完，组织已经发生了非线性位移，你的系统怎么做闭环修正？
- 用扩散模型处理多峰分布举一个手术中的例子？面对两种都合理的避障路径，模型是怎么避免决策摇摆？
- 手术动辄几小时，Transformer 的 KV Cache 会随时间爆炸，虽然换了 Mamba。但 Mamba 的线性压缩本质上是 Lossy，怎么保证它在记录了前一个小时的操作后，还能精准记得现在对应的逻辑，而不是产生幻觉？
- 刚才提到了 Transformer 和 Mamba，然后你能讲一下这两个结构的一个区别吗？
- 解释一下为什么 Transformer 是 N 的平方，然后 Mamba 是线性的。
- 有没有做过定量的实验，就是 Mamba 会相比的性能上会有变化吗？就在精度上。
- 你刚才讲到你这里其实还有一个高层的一个规划模型，这个模型是用什么做的呢？
- 输入多少张历史图片呢，如果就是只做就是做那个高层的那个规划模型，它只用三到五张，它能记住之前的所有那些操作吗，为什么？
- 整个这个高层模型的训练是怎么做的？
- 在 Mamba 的 Selective Scan 里，是怎么定义“什么该忘、什么该记”的？如果系统不小心把一个转瞬即逝的微小的点给遗忘了，怎么办？
- 如果是 Occlusion 导致的失败，能不能实现盲操？还是说系统只能挂起等医生？
- 仿真环境里的组织力学参数（如杨氏模量）和活体猪差异极大，你是怎么做对齐的？
- 大家都知道 Mamba 的状态压缩是线性递归，如果你在手术进行到第2小时时，突然出现了一个在训练集里从未出现的‘组织大面积塌陷’（OOD 场景），Mamba 的 Hidden State 会不会因为‘历史记忆过重’产生认知惯性，导致它强行把当前的异常状态拟合到过去的正常轨迹里？

在自回归解码中使用 KV Cache 时，key 和 value 分别缓存的是什么特征？如果在机器人视觉动作模型中启用 KV Cache，会对时序动作推理带来哪些影响？当模型采用 Group Query Attention (GQA) 或 Multi-Head Attention (MHA) 结构时，KV Cache 的存储和索引方式会有什么不同？如何避免在长序列机器人任务中因 KV Cache 过大导致显存溢出？为什么在 UniVLA 中直出式 action decoder 效果比 flow matching 更好？Self Attention 的完整计算流程是什么？注意力计算中为什么要除以 $\sqrt{d_k}$？Group Query Attention (GQA) 的结构和作用是什么？模型输出的是关节角还是末端位姿？具体自由度是多少？采用多任务混合训练，相比单任务单独微调，效果有什么差异？数据集是如何采集、预处理并接入训练流程的？整个数据链路是怎样的？训练时的硬件配置、数据量与训练时长大概是多少？你在动作解码模块做了哪些优化？为什么这么设计？有没有尝试过位置编码来增强左右手空间感知？效果如何？模型在推理时出现机械臂停滞、动作不连贯的原因是什么？Uni VLA 训练时遇到的主要瓶颈是什么？为什么无法利用历史帧？

- 介绍简历上的项目（需对简历上的每一个词语都很了解，如“鲁棒性”会被深挖）。
- 项目流程介绍：用的什么架构？数据格式是什么样的？采集的数量有多少？训练参数怎么设置？
- 模型失败的原因是什么？失败后有没有想办法改进？
- 介绍自己用过的模型的整体框架与流程，模型输入输出，是夹爪还是灵巧手？
- 如何设计的具体任务？长序列为什么成功率低？
- 数据集数采原理和方式是什么？
- 对当前 VLA 架构的思考？如果更新架构会从什么方向考虑？
- 长时序任务需要考虑什么问题？
- World Action Model 和 VLA 架构的对比？
- 对 Vibe Coding 的看法？平常不用 Vibe Coding 吗？
- pi0 和 pi0.5 的区别？代码上有什么区别？
- pi 和 gt00t 的流匹配的区别？
- 描述智元 LAPA 工作。
- ACT 训练和推理的流程。
- RTC 有没有做过，如何实现？
- 模型预训练用什么开源数据？
- VLM 相关内容考察：如 Transformer, ViT, 自回归生成的整个流程，模型有多少B等。
- 技术方向理解：对世界模型有无了解？解释快慢系统？对数据来源的看法？
- 讲一下PPO中的log p 是怎么算的？
- 除了PPO还有哪些RL算法。（SAC、DQN、DDPG/PPDG、TD3），那再讲一下SAC的原理。
- online和offline的数据池调用，采样到了online的失败数据了怎么办？
- PPO对动作快是怎么处理的？
- 写diffusion，PPO的代码你能现在写吗？（手撕代码）
- 仿真为什么不做人在环的RL？
- diffusion是用开源的还是自己做了网络的调整？
1. 介绍一下diffusion policy的数据流。
2. 为什么做这样的框架，试过直接用RL训练吗？
3. 这个框架相比于现有的RL微调方案优势在哪？
4. 预训练成功率70% 到微调90%，成功率20%是哪来的，剩下10%的是失败在哪？有没有看过预训练策略的动作输出和残差RL的动作输出是怎么变化的？
5. 加入对力微调有何用意吗，这损失了精度？
6. sim2real的感知偏差你怎么处理？
7. PPO训练了多长时间？