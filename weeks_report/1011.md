@张天楚  

上周已做

1. 假期前，训走不准搬的数据。一直都走不好。怀疑是训练的chunk size-=100太大了。假期训了一版size=32的还没测。
2. 为了探究 速度控制走不准 这个问题，写了一版pygame的小车环境。小车的action space也是vx, vy, vyaw，并且采用一样的cmd_vel闭环控制逻辑。距离，初始位置，dt等设置也和实机搬箱差不多。
  试图回答以下问题：
  1. 到底是不是模仿学习，对分布不均但是稀有的数据，比较难学？
  2. chunk size的影响是什么？是不是速度控制需要更高频的伺服？
  3. 如何走得更准？哪种训练技术是有效的？
结论：
1. 训练时chunk size有影响。推理时chunk size影响不大
2. 同样数据量下，数据丰富度也重要

可学习的weight：
外推效果：



下周计划做
1. 下午测试size=32的走不准搬运
2. 采失败恢复数据训练失败恢复
3. 继续从训练方法入手，精进模型从现有数据中挖掘的能力
4. Maniskill，接完控制器



待讨论事项

1. 拆垛实机开整
2. 图像数据增强开整

@杨小康 
上周已做
1. 完善comACT质心轨迹控制（统一当前动作接口设计）
  1. com姿态推理成功率验证
    1. 参考https://arxiv.org/pdf/1812.07035 将姿态表示为6D表示，修复了flatten方法错误展开旋转矩阵前两列的问题，目前训练到的model学会了如何正确的朝向目标
  2. com位置推理成功率验证
    1. 训练后的模型推理时机器人容易位置走过头，回到数据原始分布上发现mpcTargetState按t=0.1秒取的delta action变换较为不稳定，原始动作数据梯度不连续下模型训练loss容易卡在某个值停滞学习，于是优化一下动作的连贯性
      1. Delta t = 0.1
      2. Delta t = 0.25
      3. Delta t = 0.25 + Savitzky–Golay 滤波 （确保在积分区域不变的情况下让梯度过渡更平滑）
    2. 添加了滤波后的delta t = 0.25 的action重新回放是可以正常搬起箱子， 和原始 delta t = 0.1 回放数据的表现保持一致，目前训练的loss没有卡在某个固定值上
      
下周计划做
1. 确保comACT的质心com delta控制模式下可以正常在isaac Sim当中复现和 "cmd_vel 和 cmd_pose"混合高层指令的成功搬运的效果，后续以此控制模式作为模型控制躯干的输出
2. 测试适配天楚的dynamic weight 调节方法到comACT 的 train 部分

待讨论事项
1. comACT质心轨迹控制，统一动作接口

@吴泽锐 
上周已做
1. 更新数转部分的代码，话题缺失不用0进行填充，代码已合dev
2. 在kuavo-ros-control分支wzr/dev/factory_isaac_sims上用行为树采数133条（3、4、5层带下蹲），并去掉模型中对图像的裁剪处理进行DP训练(horizon=8)，训练20个epoch有一定的成功率，最新模型待测
3. 去掉裁剪处理，用实机搬箱子数据集重新训练DP(horizon=8)，h不抬手，模型有点过拟合到本体状态，40条数据的训练集上，通过消融实验观察到基本在20-30个epoch之后模型都学到依赖于本体状态的行为

---
isaac_sim 搬箱子实验（20000 steps）成功率：
- 第五层：1/4
- 第四层：3/4
- 第三层：1/5
第五层手部偏低，还有一次搬到第四层，第三层偏高，经常抓空
另外，cmd_vel和cmd_pose的衔接处理也会影响成功率

---
用123条数据进行训练，三四五层的数据混合在一起：
第五层：
第四层：
第三层：
下周计划做        
1. 有两个猜测需要验证一下，horizon和数据体量对dp训练的影响有多大？
  dp系列的论文有提到增加horizon可以得到更好的实验结果，然后搬箱子在仿真中相同的horizon的表现还不错，说明horizon=8的情况下是可以学会搬箱子这个任务的，可能数据量有影响


待讨论事项


@汤赟熙 
上周已做
1. 把目前可以work的模型和贤涵完成对接，打包策略，测试了行走-搬框-放框流程；
2. 训练更稀疏历史观测的iact (e.g., [-1.0, -0.8, -0.6, -0.4, -0.2, 0.0])，待测试；
3. 更新eval代码，使其支持稀疏历史观测的模型，待测试
下周计划做        
  1. 测试稀疏历史观测的iact;
  2. 将action改为cmd_vel/cmd_pos统一为base target 轨迹;
  3. 增加一版data parallel的训练脚本，实现多卡训练，加快模型迭代；
  4. Close iACT; 开始尝试 Autoregressive + diffusion model 的效果

待讨论事项


@顾家伟 
上周已做
1. 初步实现箱子生成器，可指定生成x行y列箱子垛，且带对齐的方向随机化，正在进行debug和优化重构
2. 辅助贤涵进行真机测试

下周计划做        
1. 箱子生成器debug和重构
  1. 

待讨论事项


@周贤涵 
上周已做
1. 搭建搬箱上层编排框架，实现行走—搬箱—放箱全流程：
  - 行走模块基于YOLO和深度相机，性能比较稳定。
  - 搬箱子采用赟熙的模型，能够搬到箱子，但是手的姿态不是很好（好像数据本身就存在这样的问题），导致放箱过程中行走不稳定，此外机器人会在箱子前踱步比较长的时间
  - 放箱模块使用行为树，细节有待完善，目前已改成边走边识别TAG，不再依赖初始位置。此部分还需要继续测试。
2. 利用前段时间采集的数据训练模型，掌握组内的训练流程。
下周计划做        
1. 完成上层编排框架，服务于后续模型的测试。
2. 阅读文献，尝试优化网络结构和训练方法。

待讨论事项


@李名扬

下周计划做        


待讨论事项


上周总体讨论事项
最近节点：
9.30，单摞绿色框45层，一定范围内随机摆放
1. 不带策略测10次来回
2. 策略打包成一个有退出条件的函数（带init）
  Class GrabboxPolicy:
  def __init__:
    // 在此处初始化policy
     def run():
    // 一直跑搬箱直到策略后退结束
3. 带策略测至少5次来回
4. 走线问题

数转：
torch.codec会更快 @吴泽锐  在数据电脑上加快速度

本周总体讨论事项
下周要有结论的事情：
1. 模型加上失败恢复能力
2. 图像数据增强
3. 拆垛采数 遥操+原子技能
4. Action space 

确认stance的切换机制
