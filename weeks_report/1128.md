@张天楚  

上周已做
1. 合并了一版kuavo-manip dev
2. 训练右边灰绿拆垛，只搬箱，和拆4611箱。成功率都比较高，约80%
3. 尝试某种act的平滑方法，效果不佳
4. 把task index放到模型输入里，正在训练


下周计划做
1. 




待讨论事项

接下来的重点事情:
1. 动作效率（重要）
2. 能处理多层（带下蹲）(打听一下轮臂)
3. 模型能处理更多OOD的情况。

@杨小康 
上周已做
1. comACT dev版本 
  1. 整理适配resetnet comACT版本到dev分支中，重新训了一版dev训练框架的comACT，修复训练后期梯度异常的问题（训练文件的不统一导致clip grad norm 梯度裁剪参数缺失）
2. comACT v0.1.0 - 左边拆垛训练及推理验证
  1. 成功率和预期相符，在左边拆垛任务上ACT成功率较高（绿色/灰色）
  2. 实验记录：11_22 - 多模型测试eval
3. Groot VLA model 
  1. 将现有的5种任务（左右绿灰拆，绿搬），完成SigLIP Vision Model 的 vision encoder微调及实物验证推理对比
  2. 将现有的5种任务（左右绿灰拆，绿搬），完成full-tune的全量微调，在验证时会发现比如在pick green box的任务上虽然都是双手同时去抓吗，但是model自己会涌现一些数据集内部不包含的能力比如左手先抓+不拉动 + 右手后抓去抓单个箱子
  3. 实验结论:11_20 Groot Eval , Groot可以很好的吸收多种 task 的模态，成功率相较于ACT来说稍显逊色，但是在舒适区内成功率在60% 70%左右，一些抓不准的原因来源于一边手抓取过后另外一边手没有拟合到箱子的宽度反而停留在了箱子外部（原因需查明）
4. 基于Qwen/Qwen2.5-VL-7B-Instruct 和 prompt 写了一个任务分类工具，便于Groot在推理的时候输入task language，不需要每次推理时候手动给定language
下周计划做
1. 将Groot当中的flow_matching_action_head分为多头机制，手臂和夹爪分头，对齐现有的recipe
2. 

待讨论事项
1. 目前已有一个稳定的VLA model可以吸收多task任务信息，是否需要整合现有的所有关于kuavo本体的数据先full-tune一个kuavo vla， 方便后续下游任务微调
2. dagger的数采框架目前如何再优化设计提高模型成功率？除了离线采集失败恢复数据之外

@吴泽锐 
上周已做
1. MT-7拆垛、搬箱数据补充，灰色箱子、绿色4611
2. 数采代码整理，便于统一dev
3. DEV上DINO版本ComACT适配
4. ComACT拆垛任务训练
  1. 左拆绿色箱子
    - chunk_size=24 成功率 2/2
    - chunk_size=20 成功率 0/4
  2. 拆+搬260数据混训
    - 没有学习到对应不同视觉输入的三种轨迹
    - 前半段轨迹会对应图像有所区别，2s左右均过渡到双手搬箱轨迹，且抖动较大
5. 任务分类器（ResNet50）
  1. V1版本
    - 取前两帧并裁剪部分带干扰背景
    - 能区分左右拆垛，但不区分搬箱和拆垛
  2. V2版本 ongoing
    - 重新采集干净背景的图像制作数据集，将数据量从1.5k扩充到20k
    - 训练时进行数据增强
下周计划做        
1. 任务分类器集成（P0）
2. 多任务训练方案（P1）
3. 个人分支整理，与DEV功能同步（P2）

待讨论事项
1. ACT怎么训多任务？还是换策略？任务分类器只能过渡，整个模型太笨重且会增加集成的工作量
2. 如果用VLA可能任务成功率比较难达到项目需求？

@汤赟熙 
上周已做
1. 模型调优，主要在新的奥比中光的原子策略数据上；裁剪过滤数据后，重新训练调整了iACT和flow-matching policy；
2. iACT与flow-matching表现接近，在舒适区（数据分布内）有60～70%的成功率；flow-matching在左右拆垛数据混训的情况下，左/右拆垛与单训效果接近，偶发出现模式跳变的情况；
3. 增加数据分析工具，用以数据集里的箱子位置分布（已合并到dev）；在后期比较严谨测试模型效果的时候，可以在模型推理开始时，记录本次trial箱子的位姿，判断该测试成功/失败是在数据分布内还是数据分布外。

下周计划做        
1. 测试基于eef的策略效果
2. 

待讨论事项


@顾家伟 
上周已做
1. 对新dev数转脚本针对pi0.5数据格式要求进行适配，数转时把每张图像提前 resize-with-pad 到模型期望分辨率（224×224，保持长宽比并 pad）以提高训练效率，
2. pi0.5侧适配resize数据集，并经过测试后完成数转
3. 配好环境下载好权重后正在微调pi0.5
下周计划做        
1. 使用现有数据集进行训练，并在仿真中推理查看效果

待讨论事项


@周贤涵 
上周已做
1. Orin ACT部署：
  1. 使用离线数据测试两种模型格式的输出是否一致
  2. 实机部署ACT搬箱模型和6D pose estimation，测试模型推理效果和速度
  3. 整理并输出实验文档：JETSON AGX ORIN部署实验文档
2. 苏州拆垛数据训练以及实机部署：
  1. 数据预处理：裁剪无效数据以及降采样
  2. 训练ACT拆垛模型并部署：
[图片]
暂时无法在飞书文档外展示此内容
  
下周计划做        
1. 测试降采样后模型的推理效果

待讨论事项


@李名扬 

下周计划做        


待讨论事项


@郭子毅 
上周已做
1.pytree完成带走路的调试，走到目标位置后不再进行变换位置。
2.IK的api调用操作，完成精准度测试，完成键盘操作实机的尝试。
下周计划做        
1.IK和采数脚本融合，并实机采集数据

上周总体讨论事项

1. 不准：ik求解仿真验证，可视化（问四书的ik版本，zujian版本，两阶段ik）
2. Dagger，更自动化一点
3. VR来做对准
4. （视觉伺服）
5. （碰撞检测）
6. 模型：eef，joint增量，act chunk 间跳变

下周总体讨论事项
1. 工具链：
  1. 贴图
2. 模型：
