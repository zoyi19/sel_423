@张天楚  

上周已做

1. 测试了trainable weight的，发现有比不加trainable weight的走得好一点。但是，如果箱子角度偏左，会南辕北辙。后来发现那个机器相机反了，但是还没重新测，会后测一下。
2. 试了SAM的encoder，但是训的很慢而且很大。loss也不如之前的。
3. 修改dataset变成能存放pretrained 的image feature。改下来速度快很多。80个episode的数据，2小时能训完250个epoch。loss和dataset上的eval效果还可以。待会后实机eval。
[图片]


下周计划做
1. 只训原地失败恢复，第五层
2. 训四五层，在（10cm，20度）范围内原地搬箱



待讨论事项

1. 分阶段：
  1. 原地手部动作
  2. 原地上下蹲+手
  3. 走路+上下蹲+手

@杨小康 
上周已做
1. comACT仿真
  1. 测试数据配置
    1. 仿真数据：20条（10条green绿色第三层， 10条green绿色第五层）
  2. 适配dynamic weight的方法，对比了平均权重的训练结果，发现同样在相同的epoch下，模型的推理预测的准确性会比平均权重高很多
  3. 更改训练和推理的方式，训练的时候每一个state模型，模型预测chunk_size为16的动作增量，推理的时候为了防止累计误差，每次推理的时候都根据当前质心状态重新推理chunk增量，丢弃之前的回放动作的动作历史功能，目前可以正常在仿真中完成搬箱任务
  4. 详细Experiment / Model设计 - 查看文档 (ComACT TestLog - Sim/Real)
2. comACT实物测试：
  1. 测试数据配置
    1. 实物数据：23条（采集了35条实机带质心话题的数据，数据过滤后23条可用）
    2. 仿真数据： 7条
  2. 实物测试当中发现了一些问题，机器人在实际执行上时，会有一个执行上的误差，对比仿真当中区域一条直线上，实物会有更多的折线（意味着执行不准确）
[图片]
  3. 根据此仿真/实物上的实验现象不同，正在以如下指标设计对比工具 "ACT在推理一段chunk_size为16的质心轨迹delta量后，下发给机器人执行，机器人执行这一段chunk的位置和姿态上的误差会是多少？"
[图片]

下周计划做
1. comACT - 模型改动补充
  1. 增加观测历史
  2. State补充
    1. 手臂末端（基于基座的）EEF pose
    2. 手臂关节速度
    3. 把IMU的状态感知换成com质心状态感知
  3. Relative 动作模式
2. 拆垛 - 模块 
  1. 实机遥操补充30条真机拆垛数据
  2. Train iACT 同时推理验证效果

待讨论事项
1. 

@吴泽锐 
上周已做
1. kuavo-manip更新
  1. 更新dp策略，去掉crop_shape并使用预训练resnet18
  2. 修复recorder，重构使用的recorder版本是之前isaac_sim版的有问题
  3. 增加行为树搬箱子失败恢复案例（第五层√，下午把检测tag的相机换成胸部采第四层）
2. dp消融实验
  验证horizon和数据量对dp训练的影：
  1. Train DP with sim data x 40 (horizon=8, 带下蹲)
  表现：20个epoch左右就有一些成功率 √ 
  分析：encoder没有使用预训练的resnet，可能仿真视觉特征比较丰富能让模型学到视觉伺服
暂时无法在飞书文档外展示此内容

---
  下面的实验仿真数据去掉了额外的视觉特征

---
  2. Train DP with sim data x 40 (pretrained, horizon=8, remove external things in sight)
  表现：30个epoch，提前抬手，无法成功抓取
暂时无法在飞书文档外展示此内容
  3. Train DP with sim data x 40 (w/o pretrained, horizon=8, remove external things in sight)
  表现：
    - 30个epoch：没有提前抬手，但是卡在抓取阶段，并拢不到位无法成功抓取
暂时无法在飞书文档外展示此内容
    - 90个epoch：没有提前抬手，成功抓取
暂时无法在飞书文档外展示此内容
  4. Train DP with real data x 78 (pretrained, horizon=8)
暂时无法在飞书文档外展示此内容
  
下周计划做        
1. 


待讨论事项


@汤赟熙 
上周已做

1. 训练测试了更稀疏历史观测的iact (e.g., [-1.0, -0.8, -0.6, -0.4, -0.2, 0.0])；单层（仅第四层）效果有提升，三四层混合训练的policy，在手末端的高度上有点分不清，偶尔搬到中间（可能是训练不足，接着再继续训一波）；
2. 更新eval代码，使其支持稀疏历史观测的模型，已测试。在cmd_vel和cmd_pos的切换上，如果关闭cmd_vel只测原地搬箱, 需要注意控制pitch角度，否则机器人会有明显的前倾；
3. 增加一版了data parallel的训练脚本，实现多卡训练，加快模型迭代；(但是现在莫得卡)
4. 训练了一个VQ-VAE model，可以用作后期Autoregressive 的vision encoder 以及 编码图片训policy
[图片]
[图片]

下周计划做        
  1. 搭建一个GPT-like Autoregressive model来预测关键帧
  2. 

待讨论事项


@顾家伟 
上周已做
1. 实现箱子生成器，可指定生成x行y列的五层箱子垛，且带平行对齐的方向随机化，间距可调
  1. 将生成器生成的箱子由xformprim转为rigidobject以方便发布实时位姿，但是流程报错无法删除重置，经大量测试isaac sim rigidobject 删除重置会导致机器人控制器崩溃，因而无法实现修改行列数量再生成，只能在初始设置固定行列参数生成箱子后随机朝向
  2. 实现生成箱子位姿发布
  3. 整理代码，将随机化及箱子生成器逻辑封装进类中
  https://www.lejuhub.com/highlydynamic/kuavo-ros-control/-/tree/jiawei/isaac_box_generator?ref_type=heads
暂时无法在飞书文档外展示此内容
2. 辅助小康，贤涵进行真机采数和测试
3. 开始进行遥操测试，但是机器人脚踝五号电机无法使能，送修
下周计划做        


待讨论事项


@周贤涵 
上周已做
1. 搬箱上层编排框架收尾工作：
  - 之前放箱模块使用一个TAG来实时定位，测试后发现TAG的姿态估计不稳定，导致机器人一直原地调整位置，无法流畅地向前移动。目前已改成使用两个TAG来确定放置点位置，机器人的位置为两个TAG位置的平均值，机器人的姿态由两个TAG连线的法向量确定。
  - 测试整体工作流程，发现机器人在放置点前需要几秒钟的调整时间。
2. 配置ISAAC仿真环境以及训练模型;
  - 在仿真环境中采集了20条搬第三层的数据
  - 使用iact模型训练：
[图片]
  loss最低为0.06，模型的测试效果不佳。打算加入一些wbc的数据重新训练。
下周计划做        
1. 对实机四五层搬箱数据做数据增强，扣除箱子上方的二维码
2. 利用增强后的数据训练iact模型，并在实机上做测试

待讨论事项


@李名扬 
- 注入空间理解和VLA文献调研
- 阅读ACT项目的代码

下周计划做        
- 看和跑Spatial Forcing的项目和代码
- ACT和VGGT代码阅读和融合
- 跑通 ACT 训练代码

待讨论事项


上周总体讨论事项
有结论的事情：
1. 模型加上失败恢复能力
2. 图像数据增强
3. 拆垛采数 遥操+原子技能
4. Action space 

确认stance的切换机制

本周总体讨论事项
事项优先级：
1. 45层，无位置变化，内部验证成功率 
2. 45层，小范围内位置变化（10cm,20度），夹板能搬起来 
3. 搬起来过程中失败恢复：初始位置随机、回合长度、


4. 夹爪，搬和拆垛，简单的两落框密堆情况
5. 不同尺寸大小框的原地搬起
6. 背景适应的随机化 
7. 能搬第三四层，改动作空间
8. 2*2复杂密堆情况拆垛
