@张天楚 

上周已做
1. 和泽锐对接训练场采搬箱数据，目前有80条原子策略搬第五层+80条vr遥操搬第五层。他们基本上一天能产出100条这样的数据。看看怎么用起来。
2. 底层控制代码同步为最新dev
3. 走不准搬运的数据，实现手柄干预逻辑并采集30条。训练了一版混合，效果不好（确实是走不准也能抬手搬了，但是也走得更不准了）。而且训练集上就能看出，没有学会转弯。
4. 改了数据混合方式重新训练一版还未测试

下周计划做


待讨论事项

1. 训练场目前头部用的是奥比中光335L。是否要统一头部相机？
2. Maniskill国庆回来再搞 
3. 

@杨小康 
上周已做
1. Isaac SIm 夹爪抓取箱子pytree适配：
  1. 末端三角夹板仿真采数案例适配pytree策略
  2. 带四指夹爪的4pro机器人目前可以通过pytree策略在环境中夹取箱子，采集了大概30条基础数据
暂时无法在飞书文档外展示此内容
2. comACT质心轨迹控制（统一当前动作接口设计）
  1. Dataset Prepare: 在bag包当中增加录制话题（'/humanoid/mpc/targetState'）, 这个话题是cmd_vel和cmd_pose的统一下发到robot控制质心的command， 同时添加录制（'/humanoid_mpc_gait_time_name'）用于记录步态模式
  2. Replay: 转换为lerobotDataset，时间戳对齐后， 回放质心轨迹和手臂运动轨迹以及GaitMode步态模式，可以成功搬运起箱子\
暂时无法在飞书文档外展示此内容
  3. Train: LerobotDataset当中已经包含了质心轨迹，参考UMI的action space设计，不再让模型学习自回归输出Absolute值，而是学习质心的相对偏移relative量
    1. 第一版训出来的模型，可以到达指定的位置，但是朝向不能对准，问题定位来源于质心欧拉角eular的relative量模型不是学的特别准
    2. 参考https://arxiv.org/pdf/1812.07035这篇论文的结论，对于model来说学习姿态表示的学习方法，欧拉角表示会存在梯度突变和不连续性，但是6D是更好学的，于是将relative量换成了6D 表示，新训好的模型待推理验证 
3. Hil-Serl: 
  1. Actor 序列 ACT warmup效果查明到是数据的原因，之前的视觉数据因为没有更改新的末端夹具所以会导致训练效果不佳，重新更换了50条末端夹具，现在warm up ACT的情况下推理可以达到和Hil-Serl论文提到的大约有50%的成功率，剩下就是post-trian
下周计划做
1. 

待讨论事项
1. comACT质心轨迹控制，统一动作接口

@吴泽锐 
上周已做
1. 江苏数采场地原子策略搬箱子采数技术支持，文档待更新
2. 更换hub并采集80条4、5层带下蹲的数据，换hub后没出现手柄问题
3. kuavo-manip四摄版，在重构的kuavo-manip上进行功能验证，包括兼容dp、iact和iact-dino等模型的训练，以及断点续训和离线推理等功能，优化数据构建部分，现在可以支持单个和多个lerobot数据集的训练
4. 四摄版dp验证，训练300个epoch，只在50个epoch之前保存的模型学到完整的搬箱子（手不准），后面的模型都趋向于对准箱子，在训练集上验证可以看出有点过依赖state，用dataset上的image和state可以正常抬手，如果用实际的state就会对准箱子，最后才突然抬手
下周计划做        
1. 更新数转部分，当前缺少相关话题的时候不会报错


待讨论事项


@汤赟熙 
上周已做
  1. 使用四摄方案采集的80条轨迹数据(第四层+第五层)训练iACT(n_obs=4, n_act=16),实物上测试下来，有一定的成功率；
暂时无法在飞书文档外展示此内容
  2. 缓解行走->抬手的动作切换卡顿问题，在推理是适当增加action_step 缓解(n_acts = 16)，但是对环境变化响应会差一点;
  3. 训练iACT带上下蹲的3，4层搬框模型(80条)，效果比较差；(走不准 & 提前抬手 & cmd_vel/cmd_pos手动切换问题)
暂时无法在飞书文档外展示此内容

下周计划做        
  1. 继续测试&改进带躯干上下蹲起的搬框模型；
  2. 将目前work的模型和贤涵对接，测试部分流程；
  3. 继续maniskill的环境和任务搭建；

待讨论事项
胸部相机 -> 广角？
Action space的统一

@顾家伟 
上周已做
1. 实现 Isaaclab 采数场景reset箱子方向随机化且对齐
[图片]
2. 完整跑通数据收集流程，确保数据收集的种类完整（和小康对齐），并可用foxglove可视化
3. 分析搬箱转不准及搬后回原点摔倒问题，发现可能是台式机性能不够导致的通信时延问题，经过测试，降低isaac lab渲染开销（渲染频率）可以大幅减少异常问题。
4. 开发箱子生成器，可指定生成x行y列箱子垛，且带对齐的方向随机化（进行中）
下周计划做        


待讨论事项


@周贤涵 
上周已做
1. 完善Maniskill的搬箱仿真环境，现在能够加载多种类型的箱子，并且随机初始化箱子的位置。
2. 搭建搬箱上层编排框架，已实现在放置点和箱子之间往返运动，待加入模型做后续测试。
下周计划做        

待讨论事项


@李名扬
上周已做
- SimpleVLA-RL: Scaling VLA Training via Reinforcement Learning
[图片]
使用一整个轨迹的成功与否对action token进行均匀奖励，但这个过程其实忽略了轨迹中可能并不是每个动作都是有用的，能否有一个更加细粒化的策略；奖励的稀疏性导致如果模型的初始能力或者泛化不足，那么RL很难起到作用；这篇文章里面提到的RL并行就是相当于Agent在多个环境并行交互进行采样和更新；我觉得这篇文章本质上就是改了一下GRPO
- OpenVLA: An Open-Source Vision-Language-Action Model
[图片]
- CoT-VLA: Visual Chain-of-Thought Reasoning for Vision-Language-Action Models
[图片]
- Octo: An Open-Source Generalist Robot Policy
[图片]
- Chain-of-Action: Trajectory Autoregressive Modeling for Robotic Manipulation
[图片]
- GR-3 Technical Report
[图片]
- Fine-Tuning Vision-Language-Action Models: Optimizing Speed and Success
[图片]

下周计划做        
1. VLA和RL微调论文的泛读 是否有更好的RL策略

待讨论事项


总体讨论事项
最近节点：
9月27，单摞绿色框搬345层，一定范围内随机摆放，80%成功率
9.30，单摞绿色框45层，一定范围内随机摆放
1. 不带策略测10次来回
2. 策略打包成一个有退出条件的函数（带init）
  Class GrabboxPolicy:
  def __init__:
    // 在此处初始化policy
     def run():
    // 一直跑搬箱直到策略后退结束
3. 带策略测至少5次来回
4. 走线问题

数转：
torch.codec会更快 @吴泽锐  在数据电脑上加快速度

