个人工作
@张天楚 

上周已做
1. 为了解决手搬下层会高，搬上层会低问题，训动手的head同时输出一个分类，指向是在搬哪一层，训练时用，推理不用。但是效果不好，且影响到了手动作的回归
2. 行为树+多线程版本的搬框案例，动作间切换停顿更少。gazebo中搬放一轮从102s 减少到65s。之后需要采数可以用这个版本。
暂时无法在飞书文档外展示此内容
3. 去掉了输入中的wbc 世界系位置，和去掉了wrench的输出，同样的数据集和训练参数。手学到突然抬起来到终点。--- 待排查。
下周计划做
1. 去掉多余观测和输出后手动不好问题排查
2. 集成rerun可视化，代码整理，提高易用性
3. 加入数据增强训练


待讨论事项

1. 

@杨小康 
上周已做
1. Hil-Serl - ACT + RL 后验
  - 解决actor训练的时候进程内存不够崩溃的问题（降低ACT模型hidden layer层数）
  - 目前参考Q-chunking在动作序列下进行训练，但是训练到后面会容易梯度崩掉，需要进一步调节超参和排查
2. RDT-1B
  1. 参考BD的LBM的架构，找到了目前比较好的RDT-1B模型，在仿真中用完善好的自动采数脚本采集50条数据在上面进行finetune
    1. 训练卡：智星云4090Plus
    2. 训练时长：28h
    3. train_step: 20K 
    4. btach_size = 16 （原配置为32）
  2. 目前20K的效果仅仅只能支撑机器人走向箱子目标，但是抬手的动作暂时没学会，需要加长训练时长测试效果（考虑加卡加数据，原论讲到需要150K才会有初步效果）
3. Lerobot - pi0-fast
  1. 将上面采集到的数据也训练了pi0-fast，目前在200K的效果下，机器人也只学习到了走向箱子，走向箱子时手部会出现一些抖动的动作（手部）
    1. 训练卡：4090
    2. 训练市场：6h
    3. train_step: 100K
    4. batch_size  = 8 （原配置为32）
下周计划做
1. Maniskill -
  1.  biped-S60接入

待讨论事项


@吴泽锐 
上周已做
1. PICO全身遥操适配VR软件脚部检测更新算法，抬脚阈值设定和一些UI的bug已反馈给软件那边，分支需要明富那边测试完再合dev
2. Kuavo-manip 接入 DP 系列 Pipeline相关工作 
  测试了n_obs_step=2,4,8的模型，都没有学到抬手，到箱子前如果视野没变化手就不动。增加数据量、调整学习率都没有改善，核心问题在以下两个方面：
  - 训练数据中相同的视觉输入有不同的动作，保持静止的数据占比很大，导致模型推理到箱子前如果视野没有变化就不抬手，人为改变箱子位置才会有抬手动作，已对无效数据进行裁剪并重新训练训练数据修正
  - horizon和n_obs_step的设置太小，默认分别是16和2，无法克服训练数据存在的动作不连续等问题，horizon调整到64重新训练，n_obs_step没有办法调的很大，之前尝试调到16显存不够，n_obs_step=8，batch_size=48就需要24G显存，而且n_obs_step=8的情况训练崩了

下周计划做        
1. DP验证
2. 

待讨论事项
加胸口的相机采数？还有原子策略的停顿太多需要优化一下

@汤赟熙 
上周已做
  A. 增加历史观测信息改进ACT（iACT），
  B. 在真机上验证测试iACT，通过增加n_obs帧数，走路比较可靠，可以稳定走到箱子面前；但是依旧存在无法在行走结束后，成功切换到抬手动作。

下周计划做        
  1. 模型层面：增加chunk_size， 重新测试iACT在不同chunk_size下，对长程动作的影响；
  2. 数据层面：重新采数据，尽量减少抬手搬箱前的停顿时间
  3. 是否有机会增加一个更低视角的相机(类似胸部相机)，使得机器人能观测到手部和箱的交互过程(目前在开始搬箱的前几秒，相机的obs几乎没变化)

待讨论事项


@顾家伟 
上周已做
1. 半自动打mask工具。可以方便抠出前景，对背景做数据增强。
暂时无法在飞书文档外展示此内容

暂时无法在飞书文档外展示此内容

[图片]

下周计划做        
1. 完善图像数据增强工具
2. 带夹爪用原子技能操作箱子

待讨论事项


@周贤涵 
上周已做
1. 熟悉机器人实机使用流程
2. 结合YOLO和深度相机输出机器人位置信息，输出的方向信息基本准确，但当机器人离箱子较远时，深度图像噪声较大，导致模型估计出的距离信息不准确。
3. 阅读了Mobile ALOHA论文
[图片]
[图片]
暂时无法在飞书文档外展示此内容

下周计划做        
1. 解决深度相机的噪声问题，完善位置估计模型
2. 阅读Diffusion Policy、Precise and Dexterous Robotic Manipulation via Human-in-the-Loop Reinforcement Learning
3. 熟悉Kuavo模仿学习代码

待讨论事项

总体讨论事项
数据自检：bag包的相机频率不一致等，做检测指标
数据replay：实机测试看偏差情况，有问题需要排查
数据采集测试：手柄遥控行走+按键触发搬框动作 -- 如果不行，改进cmd_vel
低视角相机：维修装胸口相机，打胶

p0
1. 搬运动作流畅性
2. 远距离走和大角度旋转，都用上层策略调度发指令；上层策略基于6d pose和slam等手写规则

p1
3. 人递出箱子的时候，机器人自动伸手接箱子；人伸手去拿箱子，机器人自动松开【人机交互、智能递送】
4. 行走对准的时候要能适应一定范围的对准角度，不能让机器人在框前面反复转动和移动来对准

p2
1. 抓箱子的时候，把箱子拉走，机器人重新行走对位，重新抓箱子；推车上的箱子送过来，自动开始搬运
2. 手部抓取过程中的失败恢复；肩膀左右加相机


