个人工作
@张天楚 

上周已做
1. 原子技能的维护
2. 完成了实机主从机配置和推理。推理了之前10条真机数据训的act policy，没有走到箱子前停下来。可能是没有泛化。
3. 改变任务，不学从远处approach了，而是在视野范围内转向合适位置并搬箱。改策略为根据箱子四个角的二维码中任意一个来确定箱子位置。

下周计划做
1. 采集视野范围内微调位置并搬箱的真机数据训练，并有一定效果。


待讨论事项
1. 

@汪正涛 
上周已做
1. 简化 kmpc 包，去除无关ros包依赖，避免软件组ci同步到rl仓库时打包无用的ros包
2. 四元数eskf滤波整合进kmpc-vr工作pipeline，在launch文件中可以轻松切换是否启用双手未来位姿预测以及设置预测时间，提高流畅性。下图绿色部分为预测0.1s后的姿态，红色为滤波后的位姿
[图片]
[图片]
3. 学习全身操作rl库
4. kmpc回放功能


下周计划做
1. 与清波、子瑜协作开展全身带载荷操作任务，负责带载荷部分的训练

待讨论事项
是否需要基于learning的方法做遥操作映射，解决构型空间和任务空间不协调的矛盾，因为人类演示数据对后续任务相当重要（短期内解决延迟问题）

@杨小康 
上周已做
1. Hil-Serl 仿真实验：目前在带下肢mpc运动的情况下采集了20条demo transitions（数据格式对齐lerobot dataset），然后on-line训练了两轮（每轮20分钟），在保持人工干预下的20分钟的熵值是逐渐降低的，但是这个policy没能降到一个很好的稳定程度，预计整体训练下预计要人工干预保持一个小时才有效果（当前的obs包含了物体姿态，action当中包含了关节层角度）
     2025/7/18 - Hil-Serl Kuavo Simulation测试日志
2. Hil-Serl仿真实验：针对上面agent没能很快收敛的问题，同时为了防止下肢运动的时候不稳定的问题（在随机action sample的时候会容易跑飞），直接将控制器设置为半身模式，在Lab当中设置fixed base，同时action输出增加smooth平滑项，针对搬箱子的长程任务，将obs当中去除掉姿态的观测项，action的输出使用笛卡尔系输出，不再采用关节角输出，奖励由稀疏修改为稠密，新的改动已经改动好，需要重新训练一轮看看效果


下周计划做
1. Hil-Serl Kuavo Simulation继续测试，在人工不干预的情况下，查看训练整体效果
2. 原子策略v2：kmpc搬箱子动作复现飞掉的bug，增加记录输出log功能（给到一个分支进行仿真测试，然后kmpc飞掉时自动记录最新保存的bag日志）

待讨论事项
在实机上的真机强化学习目前架构还是以先验的bc的policy作为先验，不以随机化参数开始训，先有个基础的pre-train model

@游桦 
上周已做        
1. 基于重构后的KMPC的异常检测
2. 排查KMPC不启动 控制不佳的BUG
下周计划做            
1. 

待讨论事项
1. 手臂乱飞、policy和target不重合：通过回放排查
2. 重构后的全身mpc 躯干z控制不准
3. 输入之后不求解

@吴泽锐 
上周已做
1. 修复获取真实脚部位置的时机、坐标系转换问题和指令下发逻辑
  1. 之前为了适配维护预测的脚部位置所以改用的odom系，实际单步单元的脚是基于base的，而且用预测的脚部位置的这种控制方式只适用仿真，实机测试下来会有累计误差，已经弃用
  2. 之前的实现方式脚部获取的真实数据总是在抬脚前的数据，后面排查出来是pub之后到抬脚会有0.6s左右的延时，第一次pub之后还有0.6s的SS状态，然后进入到第二步的计算逻辑，所以第二次pub的数据是基于第一步之前，已经修复，SS状态只允许下发一次有效的msg
2. Pico全身遥操真机测试，测试过程发现和仿真表现差距比较大，需要根据机况确定实机能稳定走路的最大步长，提供最大步长和一些用于安全步长保护的参数设置接口来适配不同的机器。（建议实际使用流程：单步接口测最大的稳定步长，然后配置好相关参数再接设备或者用bag测连续走路）
下周计划做        
1. 

待讨论事项

@高子淇 
上周已做        
1. https://www.lejuhub.com/highlydynamic/kuavoresearchlab/-/issues/118
2. 跑通openvla-oft在搬箱子数据上的微调，微调150Kstep，loss上看效果很好
3. 又采集了10条搬箱子数据用于验证，计算出的L1Loss=0.006，效果也不错，打印输出动作和gt动作看了下偏差也很小 
下周计划做            
1. 

待讨论事项
1. 

总体讨论事项

